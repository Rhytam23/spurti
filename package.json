{
  "name": "analysis-summership",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "seed": "node server/seed-demo-local.mjs",
    "seed-announcements": "node server/seed-announcements-demo.mjs",
    "setup": "npm install && npm --prefix client install && npm run build",
    "build": "npm --prefix client run build",
    "start": "node server/server.js",
    "dev": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mongoose": "^8.8.4"
  }
}
